name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version tag (e.g. v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve version
        id: ver
        shell: pwsh
        run: |
          if ('${{ github.event_name }}' -eq 'workflow_dispatch') {
            $v = '${{ github.event.inputs.version }}'
          } else {
            $v = '${{ github.ref_name }}'
          }
          if (-not $v) { throw 'Could not resolve version.' }
          "VERSION=$v" | Out-File -FilePath $env:GITHUB_ENV -Append
          "version=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Package executable
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release | Out-Null
          Copy-Item build/WorldClock.exe release/WorldClock.exe
          Compress-Archive -Path release/WorldClock.exe -DestinationPath WorldClock-${env:VERSION}-windows-x64.zip -Force

      - name: Package source
        shell: pwsh
        run: |
          $sourceRoot = "WorldClock-${env:VERSION}-source"
          New-Item -ItemType Directory -Force -Path $sourceRoot | Out-Null
          $exclude = @('.git', 'build', 'release', '.video-review')
          Get-ChildItem -Force | Where-Object { $exclude -notcontains $_.Name } | ForEach-Object {
            Copy-Item $_.FullName -Destination $sourceRoot -Recurse -Force
          }
          Compress-Archive -Path $sourceRoot -DestinationPath WorldClock-${env:VERSION}-source.zip -Force

      - name: Publish release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $notes = @"
          World Clock ${env:VERSION}

          Assets:
          - WorldClock-${env:VERSION}-windows-x64.zip: compiled executable
          - WorldClock-${env:VERSION}-source.zip: source snapshot
          "@
          gh release view $env:VERSION *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $env:VERSION `
              WorldClock-${env:VERSION}-windows-x64.zip `
              WorldClock-${env:VERSION}-source.zip `
              --title "World Clock ${env:VERSION}" `
              --notes $notes
          } else {
            gh release upload $env:VERSION `
              WorldClock-${env:VERSION}-windows-x64.zip `
              WorldClock-${env:VERSION}-source.zip `
              --clobber
          }